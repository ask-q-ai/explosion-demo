# Explosion Demo

Standalone VisMan package rooted by `q-marketplace.json`.

This package is a standalone scene source for marketplace packaging.

Standalone hosted package identity:

- GitHub repo: `ask-q-ai/explosion-demo`
- canonical source id: `github:ask-q-ai/explosion-demo`
- publish target: `main`
- github topic: `q-visman`

Current package files:

- `README.md`
- `q-marketplace.json`
- `scene.yaml`

Controls:

- click `POP`
- click `FIREBALL`
- click `SPARKS`
- click `GPU CLOUD`

Notes:

- `q-marketplace.json` is the only manifest.
- `scene.yaml` is the scene payload, not a second manifest.
